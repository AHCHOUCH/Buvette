"""Route declarations for the dashboard feature.

Routes will be added when the dashboard module is implemented. Route handlers must
remain thin and coordinate HTTP requests, validation, service calls, and
responses only.
"""
